(function (window) {
    window.__env = window.__env || {};

    // API url
    window.__env.apiUrl = 'https://{{ app_url }}/api/rest/';
    //window.__env.apiUrl = 'https://qa.carot.com/api/rest/';
    window.__env.apiV2Url = 'https://{{ app_url }}/v2/';
    // Base url
    window.__env.baseUrl = '/';

    // Whether or not to enable debug mode
    // Setting this to false will disable console output
    window.__env.enableDebug = true;
}(this));
